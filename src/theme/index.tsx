import Colors from './colors';
import Theme from './default-theme';

// types
type ColorsType = typeof Colors;
type ThemeType = typeof Theme;

export { Colors, Theme };

export type { ColorsType, ThemeType };
